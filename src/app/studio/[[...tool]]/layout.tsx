export const metadata = {
  title: "Tiar Portfolio — CMS Studio",
  description: "Content Management System untuk website portfolio Tiar Dwi Krisnanto",
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
